name: provider
about: simple data provider for load-balancing example

settings:
  - ColorAuto
  - InferSubcommands

args:
  - port:
      short: p
      long: port
      takes_value: true
      required: true